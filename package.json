{
  "name": "earnx",
  "version": "1.0.0",
  "private": true,
  "description": "EarnX rewards app",
  "scripts": {
    "build": "echo 'Static web app - no web build step required'",
    "cap:sync": "cap sync",
    "cap:open": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.3",
    "@capacitor/core": "^7.4.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3"
  }
}
